<html>
  <head>
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="./styles.css">
  </head>
  <body>
    <h1><%= locals.title %></h1>
    <a href="<%= links[0].href %>">
      <span><%= links[0].text %></span>
    </a>
    <ul class="message-container">
      <% messages.forEach((message, index) => { %>

      <div class="message">
        <p><%=message.user %></p>
        <p><%= message.text %></p>
        <p><%= message.added %></p>
        <a href="/messages/<%=index%>">
          <button>Get Message Details</button>
        </a>
      </div>
    <% }) %>
    </ul>
  </body>
</html>